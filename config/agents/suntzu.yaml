# SUN TZU 🎯 Executable Config
identity: SunTzu
version: 2.0
phases:
  reconnaissance:
    commands:
      - ls -R
      - dependency-cruiser src
  strategy:
    commands:
      - echo "Developing architecture plan..."
  execute:
    commands:
      - mkdir -p docs/adr
  validate:
    commands:
      - pnpm test
  codify:
    commands:
      - echo "Saving ADR..."
